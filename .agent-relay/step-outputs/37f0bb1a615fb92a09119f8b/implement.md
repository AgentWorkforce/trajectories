Created `trail-viewer/Sources/Views/Detail/TrajectoryHeaderView.swift` and wrote the complete SwiftUI implementation to disk.

Artifacts produced:
- `trail-viewer/Sources/Views/Detail/TrajectoryHeaderView.swift`

Notes:
- The view uses the real `Trajectory`, `TaskReference`, and `AgentParticipation` models from `TrajectoryModels.swift`.
- It includes both preview variants from the spec.
- I adjusted the preview background token from `Theme.page` to `Theme.pageBg` to match the actual theme API in this repo.

Verification:
- Confirmed the file exists and contains the expected Swift source.
- No build/test run was performed.

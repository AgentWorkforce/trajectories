Created `trail-viewer/Sources/Views/Sidebar/SidebarHeader.swift` and wrote the SwiftUI implementation from the provided spec to disk.

Summary:
- Added `SidebarHeader` with the serif title, `RuleLine`, conditional stats text, theme spacing/colors, and preview.
- Verified the file contents match the spec.
- Artifact produced: `trail-viewer/Sources/Views/Sidebar/SidebarHeader.swift`

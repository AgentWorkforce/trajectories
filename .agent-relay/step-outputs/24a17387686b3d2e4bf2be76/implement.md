Created  hapterNavigation.swift](/Users/khaliqgant/Projects/AgentWorkforce/trajectories/trail-viewer/Sources/Views/Detail/ChapterNavigation.swift) and ensured the `trail-viewer/Sources/Views/Detail/` directory exists.

The file contains the SwiftUI `ChapterNavigation` view, the private `ChapterPill`, a preview, and local fallbacks for `Theme.rule` and `Typography.caption`. The implementation was adjusted to use the project’s existing `Chapter` model instead of redefining it, so the preview data matches the real initializer.

Summary: 1 file created on disk at `trail-viewer/Sources/Views/Detail/ChapterNavigation.swift`.

Created [preview-generator.ts](/Users/khaliqgant/Projects/AgentWorkforce/trajectories/trail-viewer/server/src/preview-generator.ts).

It implements `generatePreview()` and `generatePreviewsForAll()` with self-contained HTML rendering, recursive JSON discovery, mtime-based skipping, chapter summaries, decision blocks, retrospective rendering, and footer metadata. Verified the file itself with `npx tsc --noEmit --target ES2022 --module ESNext --moduleResolution bundler trail-viewer/server/src/preview-generator.ts`.
